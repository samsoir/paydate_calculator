#include <stdio.h>
#include "../include/paydate_calculator.h"

int main()
{
  pc_execution_mode_t mode = PC_EXECUTION_MODE_MONTH;
  printf("Hello cruel world!\nExecuting in mode: %i\n", mode);
  return 0;
}
